apiVersion: v1
kind: Service
metadata:
  name: master-service-clusterip
  namespace: code-executor
spec:
  selector:
    app: master
  ports:
    - protocol: TCP
      port: 9001
      targetPort: 9001
  type: ClusterIP
